# Legacy Next App

This folder contains legacy Next-style app code moved from `src/app` during the React Router feature-structure refactor.

- Active frontend entrypoints are under `src/pages/*` and wired through `src/App.jsx`.
- Files here are intentionally isolated and currently inactive.
- Cleanup or migration of this legacy code should happen in a dedicated follow-up change.
