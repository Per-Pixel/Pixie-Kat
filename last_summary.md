# Last Summary

## Session: About mobile pin layout + ship

### Done
- Homepage About stays CMS-wired (copy/image only) with pristine GSAP clip timeline
- Mobile: mask near top of `#clip` (`top-4`, `55vh`, max `85vw`) to close text→image gap; desktop unchanged (`top-0`, `60vh`, `30vw`)
- `#clip` uses `overflow-hidden` + `ScrollTrigger.refresh` after CMS/fonts/image load
- Committed About-only files and pushed to `origin/main` + `origin/admin`

### Left uncommitted (other work)
- Games catalog API fallback / RLS helper migration / related hook changes
- `About.jsx.backup` (local only)
